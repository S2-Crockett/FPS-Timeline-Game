using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System;

public class ObjectiveComplete : MonoBehaviour
{
    /*private MiniObjective miniobjectivescript;

    void Start()
    {
        miniobjectivescript = GetComponent<MiniObjective>();
    }

    void OnTriggerEnter(Collider other)
    {
        if (other.gameObject.tag == "Player")
        {
            miniobjectivescript.MiniObjectviveAppear = false;
            Debug.Log("Mini Objective complete");
        }
    }*/
}